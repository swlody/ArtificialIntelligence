Run make to compile. Run ./TicTacToe to play and follow instructions. A more thorough write-up is contained in ../Report.pdf

I was lazy with input validation so don't enter a string when it asks for an integer thanks.